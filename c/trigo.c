#include <stdio.h>
#include <math.h>
int main(){
    float ang;
    scanf("%d",&ang);
    printf("the cosine of the angle is : %f",cos(ang));
    printf("the sine of the angle is : %f",sin(ang));
    printf("the tangent of the angle is : %f",tan(ang));
    //printf("the cosec of the angle is : %f",cosec(ang));
  //  printf("the cosine of the angle is : %f",sec(ang));
  //  printf("the cosine of the angle is : %f",cot(ang));
    return 0;

    



}